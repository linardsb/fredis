# Weekly Memory Synthesis — 2026-W26

_Drafted 2026-06-28T08:02:11+01:00 · reviewed 7 day(s) · 5 proposal(s)._

I've reviewed MEMORY.md against all 7 daily logs (2026-06-21 through 2026-06-27). The biggest finding is that the 2026-06-22 "watchdog not to build" decision was reversed and shipped on 2026-06-27, and the Claude OAuth `/login` workaround was replaced with a permanent token fix.

### Proposal: VPS OAuth watchdog decision reversed and shipped
**Type:** contradiction
**Before:** `**VPS OAuth watchdog decided not to build (decided 2026-06-22).** Offered in the 2026-06-20 session; explicitly decided against in 2026-06-22 session. Rationale: heartbeat already alerts on auth failure (the 7:32 Slack message on 22 Jun *was* the watchdog firing); building a separate probe would duplicate it; deploying new code to VPS is risky because the repo entangles vault-sync auto-commits with code on `main`, and `git pull --ff-only` can wedge. `[impact: low, status: decided]\``
**After:** `**VPS auth watchdog built and live (2026-06-27).** Decision reversed from 2026-06-22 ("don't build") after recurring 401 cascade Jun 24–26 (17 consecutive heartbeat failures from 24 Jun 17:54 through 26 Jun 12:22). Commit \`7982947\`: hourly systemd timer probes both Claude and Google credentials; Slack-pings \`#hubspot\` on genuine auth failure with inline fix steps; dedup via state helpers; recovery notice on restore. CI green, VPS timer enabled, first run confirmed \`claude=ok google=ok\`. The 2026-06-22 rationale ("heartbeat already alerts") didn't hold — the Jun 24–26 cascade showed heartbeat failures don't reach Slack when auth itself is broken (alert path requires working SDK). \`[impact: med, status: decided]\``
**Rationale:** 2026-06-27 08:52 session-end flush explicitly states "Auth watchdog built, committed (`7982947`), deployed, and live" — directly reversing the 2026-06-22 decision currently recorded in MEMORY.md. Daily logs 2026-06-24 through 2026-06-26 show 17 consecutive guardrail-failed-closed heartbeats with no Slack alert escaping, refuting the "heartbeat already alerts" rationale.

### Proposal: Claude OAuth permanent fix supersedes the `/login` workaround
**Type:** resolution
**Before:** `**VPS Claude OAuth expiry — root cause of heartbeat failures ~17–20 Jun (resolved 2026-06-20).** OAuth was silently revoked ~17 Jun; every SDK call returned 401 → heartbeat "failed closed" (exit 1, empty stderr). Fixed via full \`/login\` re-auth on VPS. Pattern confirmed: when all heartbeats fail closed with no error detail, check OAuth before debugging code. Weekly 401 watchdog probe offered but not yet actioned — see Open Watch Items. \`[impact: med, status: resolved]\``
**After:** `**VPS Claude OAuth — permanent fix shipped 2026-06-27 after second 401 cascade.** The 2026-06-20 \`/login\` re-auth held only ~4 days; auth revoked again ~24 Jun evening producing 17 consecutive heartbeat failures (24 Jun 17:54 → 26 Jun 12:22). Permanent fix: replaced recurring-OAuth login with 1-year \`CLAUDE_CODE_OAUTH_TOKEN\` setup-token; jammed old login removed; chat reconnected clean. Billed to Max sub (£0 metered). Token renewal due ~June 2027. Auth watchdog (commit \`7982947\`) now monitors the fix. Pattern confirmed: \`/login\` alone is a temporary workaround — long-lived setup-token is the durable answer. \`[impact: med, status: resolved]\``
**Rationale:** Daily logs show the 2026-06-20 fix did NOT hold — heartbeats 2026-06-24 17:54 through 2026-06-26 12:22 show 17 consecutive `guardrail failed closed (consecutive #1...#17)` with `Exception exit_code=None stderr=None`, the exact pattern MEMORY.md flags as silent OAuth death. 2026-06-27 08:52 session flush documents the setup-token replacement as the permanent fix.

### Proposal: Merge three VPS-auth entries into a single saga
**Type:** merge
**Before:** Three separate Key Decisions entries — `VPS Claude OAuth expiry — root cause of heartbeat failures ~17–20 Jun (resolved 2026-06-20)`, `VPS OAuth watchdog decided not to build (decided 2026-06-22)`, and `Google OAuth app confirmed already in Production — 7-day Testing-mode theory was wrong (2026-06-22)` — each treating an episode of the same June 2026 VPS-auth incident in isolation.
**After:** `**VPS auth saga June 2026 — recurring 401 cascade fixed permanently (resolved 2026-06-27).** Three-act incident: (1) Claude OAuth silently revoked ~17 Jun → 3-day heartbeat outage → resolved 20 Jun by \`/login\` re-auth. (2) Google OAuth also failed independently around same window — re-minted; app confirmed already in Production (the 7-day Testing-mode theory was wrong; token death was almost certainly a one-off account-level revocation). (3) \`/login\` fix only held ~4 days — Claude OAuth re-revoked ~24 Jun evening, producing 17 consecutive heartbeat failures Jun 24–26. Permanent fix shipped 27 Jun: 1-year \`CLAUDE_CODE_OAUTH_TOKEN\` setup-token + hourly auth watchdog (commit \`7982947\`) that Slack-pings \`#hubspot\` on probe failure. Key lessons (also in Lessons Learned section): check Claude + Google auth independently; \`/logout\` before \`/login\` to clear jammed tokens; \`CLAUDE_CODE_OAUTH_TOKEN\` env var overrides \`credentials.json\`. \`[impact: med, status: resolved]\``
**Rationale:** Three Key Decisions entries (2026-06-20 OAuth resolved, 2026-06-22 watchdog decided-against, 2026-06-22 Google Production confirmed) plus the now-overturned watchdog decision all describe phases of one incident now closed by the 2026-06-27 permanent fix. Merging preserves all facts while letting Linards see the arc — and removes the stale "watchdog not built" claim refuted by daily logs.

### Proposal: Email Hub kill-trigger date passed without action
**Type:** kill-trigger
**Before:** `- **Email Hub IP question — execution blocker.** Has Linards raised the IP question with Merkle legal yet? Email Hub is paused until answered. Kill trigger: no answer by month 2 → pause indefinitely (2026-04-19)`
**After:** `- **Email Hub IP question — kill trigger date passed 2026-06-19, but execution continues.** Original kill trigger from 2026-04-19: no answer by month 2 → pause indefinitely. Month 2 deadline (2026-06-19) has passed without evidence Linards raised the IP question with Merkle legal. However, Email Hub work has NOT paused — heartbeats 2026-06-23 + 2026-06-27 show active outreach: Matt Heffer + Rob Lord LinkedIn DMs drafted, reminder window opens 2026-07-07; \`email-hub.saulera.com\` site work in progress. **Decision needed:** either confirm the kill trigger is silently revoked (and update USER.md sequencing), or honour it and pause outreach. \`[impact: med, status: pending]\``
**Rationale:** 2026-04-19 set "no answer by month 2 → pause indefinitely" as a pre-committed kill trigger; today is 2026-06-28, which is past the 2026-06-19 month-2 deadline. Heartbeats 2026-06-23 and 2026-06-27 show Email Hub outreach actively continuing (Matt Heffer + Rob Lord LinkedIn DMs, July 7 reminder window), contradicting the "pause indefinitely" kill condition. The trigger needs an explicit ruling.

### Proposal: VPS heartbeat OAuth failure pattern proven recurring
**Type:** resolution
**Before:** `**VPS Claude OAuth expiry produces silent total heartbeat failure (2026-06-20).** When VPS Claude OAuth is revoked, every SDK call returns 401 → heartbeat exits 1 with empty stderr → daily log shows only "failed closed" with no error detail. The failure is indistinguishable in the log from a code bug or guardrail false-positive. Three days (~17–20 Jun) were lost before the 401 root cause was found. **Diagnostic rule:** if all heartbeats fail closed with empty stderr and no recent code change, run a bare SDK call on VPS to check for 401 before debugging code. Fix: full \`/login\` re-auth on VPS. \`[impact: med, status: decided]\``
**After:** `**VPS Claude OAuth expiry produces silent total heartbeat failure — pattern confirmed recurring (2026-06-20, recurred 2026-06-24).** When VPS Claude OAuth is revoked, every SDK call returns 401 → heartbeat exits 1 with empty stderr → daily log shows only "failed closed" with no error detail. The failure is indistinguishable in the log from a code bug or guardrail false-positive. First instance lost 3 days (~17–20 Jun); second instance lost ~36 hours (24 Jun 17:54 → 26 Jun 12:22, 17 consecutive failures). **Diagnostic rule:** if all heartbeats fail closed with empty stderr and no recent code change, run a bare SDK call on VPS to check for 401 before debugging code. **Workaround:** \`/login\` re-auth — holds ~4 days. **Permanent fix:** 1-year \`CLAUDE_CODE_OAUTH_TOKEN\` setup-token + auth watchdog (commit \`7982947\`, 2026-06-27). \`[impact: med, status: decided]\``
**Rationale:** The original Lesson Learned entry treats the OAuth failure as a one-off; daily logs 2026-06-24 through 2026-06-26 prove it recurs and the `/login` fix is temporary. Updating preserves the diagnostic rule while documenting the durable fix that now closes the loop.
