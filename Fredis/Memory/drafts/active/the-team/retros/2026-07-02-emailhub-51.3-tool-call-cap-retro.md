---
skill: integrations/dispatch-retro
lane: P4
status: draft
brief: emailhub-51.3-tool-call-cap
pr: linardsb/merkle-email-hub#309
run: 70b121661df83d9ce4ab19d1813936f2
outcome: closed-inert
evidence: full          # brief + PR + first-hand gate logs + Dispatch History all available
created: 2026-07-02
---

# Dispatch Retro — 51.3 tool-call cap (PR #309, closed-inert)

## Intent (from the brief)

Complete the K_max trio with a deterministic per-session tool-call cap sited in
`BaseAgentService.process()`, plus additive `ai.agent_decision` telemetry. Acceptance:
~6 unit tests in `test_tool_cap.py` exercising the counter, reset semantics, additive
schema, and the 503 mapping; `make lint types test` green.

## Outcome

Draft PR #309: 7 files, +290/−9. Path `rca→implement→gate1 ✗→fix→gate2 ✓→create-pr-retry`
(1 fixup). Gate green (8180 passed). **Closed unmerged at HITL #2: functionally inert** —
`_ToolCallCounter.record_tool_call()` has zero production call sites; `process()` is
single-shot, so the cap can never trip and `tool_calls_made` always logs 0. #307 stays
open as the re-plan tracker.

## Findings

```yaml
finding: Brief specced the cap onto a surface that cannot host it — process() is
  single-shot (no in-process tool loop exists anywhere; only MCP batch_execute iterates
  tool calls, driven externally). The agent implemented §51.3 faithfully; the defect is
  the brief/plan's false "K_max trio" symmetry.
locus: brief
evidence: HITL review comment on #309 (2026-07-01T19:30Z); Dispatch History root-cause
  bullet (base.py process() single-shot, timeout= wrapper at base.py:359); brief §"What
  to do" step 3 naming BaseAgentService.process() as the site.
```

```yaml
finding: Acceptance criteria validated the counter in isolation, not on the live path —
  the brief's ~6 tests all call the counter directly, so a green gate proved "the class
  works", never "the class is invoked". An end-to-end acceptance ("cap trips at N+1 on a
  real agentic run") would have failed at implement time and surfaced the plan defect
  pre-PR.
locus: brief
evidence: brief §Acceptance criteria; test_tool_cap.py invokes record_tool_call() at
  lines 61/114 (only call sites, per HITL review comment).
```

```yaml
finding: The unit gate is structurally blind to "never called on the live path" —
  ruff/mypy/pyright/pytest cannot detect a missing production call site when tests
  exercise the code directly. Gate-green ≠ wired-in for logic-wiring tasks.
locus: gate
evidence: gate2.log "8180 passed" on inert code; HITL review comment §"Why the gate
  passed".
```

```yaml
finding: Informed-retry worked as designed — gate1 failed at make types (mypy arg-type
  in test_tool_cap.py:207), fix node repaired it blind, gate2 green. 1 fixup, recovered.
  System working, not a defect. Record note: the Dispatch History bullet says "gate1
  ✗(lint)" but gate.log shows GATE FAILED at make types — minor record inaccuracy.
locus: none
evidence: artifacts/runs/70b12166…/gate.log tail; gate2.log tail.
```

## Routing

| Finding | Rail | Action taken |
|---|---|---|
| Brief specced a non-existent surface | **(c)** skill observation | Logged to `skill-observations/log.md`: integrations brief-shaping must include a **surface-existence check** — a logic-wiring brief cites the live call site (file:line) where the new code will be invoked, verified by grep, before HITL #1. |
| Isolation-only acceptance | **(c)** same observation | Acceptance for logic-wiring briefs must include one end-to-end trip test on the real execution path, not only direct-call unit tests. |
| Gate blind to unwired code | **(c)** same observation + **(d)** | Gate blindness is inherent to a unit gate; the compensating control is the brief-level check above + HITL #2 attention on logic-wiring PRs. Recorded; no YAML change (see Not taken). |
| Retry proven / record inaccuracy | **(d)** daily log | One-line retro summary appended to `daily/2026-07-02.md`; reflection promotes to Dispatch History. |

## Verdict

- **Trust read:** confirms the ceiling, doesn't move it — logic-wiring tasks are NOT
  trust-blind dispatchable; the failure mode is now precisely characterised (faithful
  implementation of an unverified plan). Mechanical lane unaffected.
- **System delta:** 1 skill observation (3 parts) + daily-log line. No command edits, no
  YAML drafts.
- **Not taken:**
  - Workflow-YAML "runtime verification node" (state-doc gap C1) — a generic node can't
    know each task's live path; the cheap, correct control is upstream in the brief
    (surface-existence + trip-test acceptance). Revisit if a brief-level check fails twice.
  - Re-dispatch of 51.3 — ruled out 2026-07-01 (plan defect; re-plan in-session, #307).
  - No rail (b) edit — no brief-shaping command exists in `.claude/commands/`; brief
    conventions live in the integrations skill, so the observation rail owns this.
