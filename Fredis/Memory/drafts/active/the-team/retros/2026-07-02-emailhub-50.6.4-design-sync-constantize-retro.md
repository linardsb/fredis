---
skill: integrations/dispatch-retro
lane: P4
status: draft
brief: emailhub-50.6.4-design-sync-constantize
pr: linardsb/merkle-email-hub#310
run: b2575b90a79c46ad83d203679d5c9d29
outcome: merged
evidence: full          # brief + PR + first-hand gate log + Dispatch History all available
created: 2026-07-02
---

# Dispatch Retro — 50.6.4 DESIGN_SYNC constantize PR-1 (PR #310, merged)

## Intent (from the brief)

PR-1 constantize subset only: move over-engineered `DesignSyncConfig` knobs to `Final`
constants in `app/design_sync/tuning.py`, rewire every consumer, delete the fields.
Hard conservative-keep rule; do-not-touch list of retire-candidates; bounded-count test
(≤45 target, with explicit permission to land above it if conservative-keep demands);
no Bash; VSA boundary `app/core/config/` + `app/design_sync/` + config test dir.

## Outcome

Draft PR #310: 8 files, +78/−46, path `rca→implement→gate1 ✓→create-pr` (**0 fixups**,
gate.log `passed=true`, 8176 tests). 8 knobs constantized, every consumer rewired,
conservative-keep held (8 of ~15; bound set to ≤57 with the ≤45 aspiration and PR-2→≤30
path documented candidly), no retire-candidate touched. HITL #2: Linards confirmed none
of the 5 env-named knobs are overridden in prod → **merged** (`3c98971a`) after #306
cleared the pre-existing dep-vuln CI reds.

## Findings

```yaml
finding: Clean run, zero gaps between brief and delivery — the conservative-keep hard
  rule and the pre-authorised count shortfall both held exactly as written. This is the
  mechanical-lane baseline working.
locus: none
evidence: PR merged; gate.log passed=true; Dispatch History N=2 bullet ("mechanically
  correct… cleared to merge").
```

```yaml
finding: Brief's VSA boundary omitted a file the repo's own gates force you to touch —
  .env.example is generated/synced (env-drift check), so deleting config fields REQUIRES
  editing it, yet the brief confined changes to three dirs. The agent did the right
  thing; the review bot (Qodo) flagged it as a compliance breach because the issue text
  carried the incomplete boundary.
locus: brief
evidence: Qodo review finding 1 on #310 (.env.example L236-238 outside approved dirs);
  brief §Scope "VSA: changes stay under app/core/config/, app/design_sync/, and the
  config test dir".
```

```yaml
finding: Brief and GitHub issue disagreed on an acceptance bound — the brief authorised
  landing above 45 under conservative-keep; the issue text (which review bots treat as
  the contract) required the exact ≤45 assertion. Same source, two contracts → automated
  reviewers generate noise HITL #2 has to discount.
locus: brief
evidence: Qodo review finding 2 on #310 ("Compliance ID 499009 requires <=45; test
  asserts <=57"); brief §Acceptance criteria explicitly permitting the shortfall.
```

```yaml
finding: Harness gate omits the review-time gates (network pip-audit + Trivy container
  scan) — full CI was red on pre-existing dep vulns (CVE-2026-27145 go-stdlib in the
  pg16 image; engineio/socketio CVEs) until #306. Already diagnosed and recorded on the
  repo page (Workflow Preferences + Dispatch History close-out bullet) — evidence only,
  nothing new to route.
locus: gate
evidence: Qodo CI-feedback comment on #309 (Trivy log); Dispatch History close-out
  bullet 2026-07-01; repo page §Workflow Preferences harness-gate paragraph.
```

## Routing

| Finding | Rail | Action taken |
|---|---|---|
| Clean baseline | **(d)** daily log | Counted in the retro summary line; reflection promotes. |
| Boundary omitted generated files | **(c)** skill observation | Logged: brief VSA-boundary lists must include repo-forced companion files (generated/synced artefacts like `.env.example`) or state "plus files the repo's own checks force in sync". |
| Brief ≠ issue on acceptance | **(c)** same observation | Brief and the GitHub issue it enriches must carry identical acceptance bounds — the issue is what review bots read; divergence turns pre-authorised judgement calls into false compliance findings. |
| Gate omits pip-audit/Trivy | none (already recorded) | Audit Conventions: recorded on the repo page 2026-07-01; re-proposing is noise. |

## Verdict

- **Trust read:** strengthens the mechanical lane — 0 fixups, judgement rule
  (conservative-keep) followed under ambiguity, candid shortfall reporting. No ceiling
  change needed.
- **System delta:** 1 skill observation (2 parts, brief-shaping) + daily-log line.
- **Not taken:**
  - Adding pip-audit/Trivy to the harness gate — network-dependent + slow, and the state
    doc already frames clean deps as a separate merge precondition; keep at review time.
  - Qodo finding 3 (deprecated env vars silently ignored at startup) — a target-repo
    product decision, not a dispatch-system defect; it belongs to Email Hub's own
    backlog, not this retro's rails.
