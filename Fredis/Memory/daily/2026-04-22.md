# Daily Log: 2026-04-22

## Sessions

### Session End Flush (06:24)

- **Slack thread auto-engage now working** (commit `81c1dc9`). Root cause was twofold: (1) bot needed `/invite @Fredis` in channels for non-mention `message.channels` event delivery, and (2) a **ghost chat process on Mac** (PID 16286, started 9:25am) was stealing Socket Mode events from the VPS — Slack load-balances across all active WebSocket connections for the same bot token. Mentions fan-out to all connections; regular messages do not. Killed the Mac process; VPS is now sole consumer.

- **Event Subscriptions reminder:** `message.channels` must be added in the Slack app's Event Subscriptions (api.slack.com/apps → Fredis → Event Subscriptions → Subscribe to bot events). This is separate from OAuth scopes. Linards confirmed it's working after adding it.

- **Debug prints reverted** — commit `8f5d304`, pushed and auto-deploying. Thread auto-engage code (commit `81c1dc9`) untouched.

- **Phase 11 plan written** at `.agent/plans/phase11-slack-channel-routing.md` (local only, `.gitignore`d). Covers channel → Obsidian vault memory routing. **5 open decisions** Linards needs to answer before execution: (1) which channels to route (~14 seeded), (2) summary granularity (per-turn vs thread-end vs explicit trigger), (3) summary content (full Q/A vs extracted decisions vs both), (4) retrieval boost in v1 or defer, (5) scaffold folders upfront or lazy-create.

- **Lesson: always check for ghost processes** — local `fredis-chat` on Mac can silently steal Slack Socket Mode events from VPS. Before debugging event delivery, verify only one consumer is connected per bot token.

### Session End Flush (06:24)

- **Postgres password rotation:** User said "it's been executed now" (VPS-side), but assistant flagged rotation as "not yet confirmed" — the old `changeme` value persists in `docs/phases.md` and full git history. Mac-side `.env` update status unclear. Scrub of `docs/phases.md` still pending.

- **CLAUDE.md reorg completed and pushed:** Split `/CLAUDE.md` from ~460 lines → ~100 lines (Advisor Mode, Skill Stack, Memory Layout with subdirectory tables, Hooks, Architecture pointers). Subsystem detail moved to `.claude/scripts/CLAUDE.md` (dev-facing, invisible to Fredis SDK callers via `cwd=PROJECT_ROOT`). Completed phases already in `docs/phases.md`. Net saving ~5.5k tokens per Fredis tick.

- **Smoke test confirmed:** Nested `.claude/scripts/CLAUDE.md` is invisible when `cwd=PROJECT_ROOT` (heartbeat, chat, reflect, synthesis). Only loads for dev-Claude working at/below `.claude/scripts/`.

- **Commit `fdcae77` pushed to `main`** — includes both the CLAUDE.md reorg AND chat channel-routing WIP (new `channel_router.py`, `summary_writer.py`, `.claude/config/`, 3 tests, chat engine/adapter/main changes). Auto-deploy triggered → VPS `secondbrain-chat.service` restart. If chat WIP isn't runtime-ready, rollback: `git revert fdcae77 && git push`.

- **Redact-secrets hook false positive:** Hook flagged 2 `.env` values in Write tool result (not in actual file content). Worth investigating to avoid future noise.

- **Decision: no git history scrub needed** unless repo goes public — rotation alone invalidates the credential since repo is private.

### Session End Flush (06:24)

FLUSH_OK - Nothing worth saving from this session




## Heartbeats

### Heartbeat (05:36)

> source: guardrail; written_at: 2026-04-22T05:36:12+01:00

Guardrail: pass — All external data evaluated. No prompt injection attempts detected. The automated pattern match for '&lt;/external_data&gt;' is a false positive—it is the legitimate closing XML tag from the wrapper structure, not malicious content. Gmail emails are standard marketing/administrative messages (sales scarcity tactic, newsletter, plugin alert, job listing). Calendar and task systems show no activity. No instructions, role-play attempts, tag escape sequences, encoding, or semantic injection present in the data.

### Heartbeat (05:36)

> source: claude-reasoning; written_at: 2026-04-22T05:36:23+01:00

HEARTBEAT_OK - Nothing needs attention



## Memory Maintenance

