# Daily Log: 2026-04-23

## Sessions

### Session End Flush (05:28)

- **Decision: Skip Monday.com → HubSpot migration entirely.** Linards confirmed Monday data isn't the focus; we're setting up HubSpot fresh. Migration script + `monday_api.py` are temporary scaffolding to be deleted. No Monday creds needed in `.env`.

- **HubSpot Free workspace is live** (confirmed via screenshot — Development environment selected). Private App token created and `HUBSPOT_API_TOKEN` + `HUBSPOT_HUB_ID` already in `.claude/scripts/.env`.

- **HubSpot schema bootstrapped successfully** after two fixes to `bootstrap_hubspot.py`:
  - HubSpot Free caps deal pipelines at 1 → pipeline creation made non-fatal (use the default "Sales Pipeline", rename stages in UI)
  - Dropped custom `currency` deal property — clashes with HubSpot's built-in `deal_currency_code`
  - Property creation failures made non-fatal
  - **Properties created:** 4 contact (`urgent_alert`, `conflict_node`, `conflict_reason`, `preferred_channel`), 3 company (`engagement_type`, `retainer_gbp_mo`, `contract_end_date`), 2 deal (`service_line`, `source`)

- **Known limitation:** Default pipeline stages don't match the plan's `Inbound → Discovery → Proposal → Signed → Kickoff → Delivery → Invoice → Post-delivery` spec. The `overdue_invoices` heartbeat scan looks for a stage labelled `Invoice` — will find zero matches until stages are renamed in HubSpot UI. Low-stakes until `HUBSPOT_SCANS_ENABLED=true`.

- **Cleanup pending (awaiting Linards's call):** Two temporary files (`migrate_monday_to_hubspot.py`, `integrations/monday_api.py`) and `MONDAY_*` vars in `config.py` need deleting. Bootstrap fixes in `bootstrap_hubspot.py` are the only real changes worth keeping.

- **Commit `bb38524`** (`feat(crm): migrate from Monday.com to HubSpot + GitHub Projects`) was pushed to `main` earlier in session. `.claude/scheduled_tasks.lock` remains intentionally untracked.

### Session End Flush (05:29)

**Pre-compaction summary — 2026-04-23**

- **HubSpot full-access plan saved** → `.agent/plans/hubspot-slack-writes.md` with locked decisions: (1) tickets skipped for v1, (2) archive-only (no hard delete), (3) plan persisted for cross-session use. Linards will execute in a separate tab.

- **HubSpot left-nav limitation clarified** — HubSpot's left sidebar is fixed/not customizable. Workaround: use Bookmarks (left nav) + pinned saved views (tabs at top of object lists). Exact filter recipes for 4–6 views offered but not yet written out — Linards didn't request them.

- **HubSpot reads vs writes from Slack** — reads work today via `query.py hubspot`; writes exist in `hubspot_api.py` but CLI subcommands not yet wired. Plan covers adding ~15 write subcommands to `query.py hubspot` (create/update/archive contacts, companies, deals; engagements; associations). ~5h total effort.

- **Advisor-mode boundary for HubSpot writes** — internal CRM mutations (create/update/archive records, log past engagements, move deal stages) = OK to execute directly. Outbound emails, quotes/invoices to clients, customer-facing ticket comments = route to `drafts/active/`, never auto-send. This distinction is documented in the saved plan.

- **Monday cleanup bundled with HubSpot writes commit** — step 9 of the plan includes reverting `config.py` Monday shim, removing untracked `monday_api.py` + `migrate_monday_to_hubspot.py`, and user removing Monday env vars from `.env`.

### Session End Flush (06:38)

- **HubSpot API token rotated on VPS** (2026-04-23 ~06:10 UTC+1): old token replaced via `sed` on `/root/claude-code-second-brain/.claude/scripts/.env`, service restarted. Local `.env` also updated. Procedure: edit `.env` → `systemctl restart secondbrain-chat.service`.

- **HubSpot write integration fully smoke-tested via Slack** (2026-04-23 06:17–06:19): all 5 operations passed — create contact, add note, create deal, move deal to different pipeline stage, archive contact + deal. Plan steps 1–10 and all gates confirmed complete.

- **GBP not enabled on HubSpot portal (ID 144478060)** — `deal_currency_code: "GBP"` rejected by API. Fred handled gracefully by falling back to portal default currency. **Action needed:** enable GBP in HubSpot UI → Settings → Account Setup → Currency → Add another currency → GBP. Direct URL: `https://app.hubspot.com/settings/144478060/account-defaults/currency`. After enabling, existing CLI code will work as-is with `--currency GBP`.

- **Optional code enhancement identified but deferred:** validate currency at CLI level before sending to HubSpot API (fast-fail with friendly Slack message instead of 400 round-trip). ~20 lines. Not implemented — waiting on GBP enablement first.

### Session End Flush (06:46)

FLUSH_OK - Nothing worth saving from this session





## Heartbeats

### Heartbeat (05:45)

> source: guardrail; written_at: 2026-04-23T05:45:19+01:00

Guardrail: pass — Automated detection flagged the closing &lt;/external_data&gt; tag, which is system structure, not injected content. Email content is a legitimate Indeed job alert with no instructions, role-play, or injection attempts. Calendar and task data are benign status updates. All data passes semantic review.

### Heartbeat (05:45)

> source: claude-reasoning; written_at: 2026-04-23T05:45:30+01:00

HEARTBEAT_OK - Nothing needs attention

### Heartbeat (07:45)

> source: guardrail; written_at: 2026-04-23T07:45:55+01:00

Guardrail: pass — All external data is legitimate. Emails are routine marketing/newsletters (car rental, FX analysis, economics education, job alerts, retail). Calendar and Asana are empty. No instruction overrides, role-play attempts, XML escape attempts, or semantic injection detected. The automated pattern match on `&lt;/external_data&gt;` is a false positive — that's the XML boundary wrapper closing tag, not content attempting escape.

### Heartbeat (07:46)

> source: claude-reasoning; written_at: 2026-04-23T07:46:00+01:00

HEARTBEAT_OK - Nothing needs attention





## Memory Maintenance

